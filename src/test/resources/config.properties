# Application under test - a protected platform.joveo.ai URL, which redirects to the login page
# (accounts.joveo.com/login) whenever there is no authenticated session. Note: this app does not
# deep-link back to the originally requested page after login - a successful login always lands
# on /jobs/jobs-overview regardless of which protected URL triggered the redirect (verified).
baseUrl=https://platform.joveo.ai/0mx92h3c99v58/settings/branding-hub

# Browser & execution settings. Override at runtime with -Dbrowser=firefox / -Dheadless=true
browser=chrome
headless=false

# Centralized timeouts (seconds). Override with -DexplicitWaitSeconds=20 etc.
explicitWaitSeconds=15
pageLoadTimeoutSeconds=30

# Valid credentials used only by the happy-path (scenario 7) login test.
valid.email=jerimeb565@mvpmedix.com
valid.password=JoveoTest@12345
